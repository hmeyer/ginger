# RUSTSEC-2024-0436: `paste` is unmaintained. It is a build-time
# proc-macro with no runtime/security surface, pulled in only via
# nalgebra -> simba -> paste. No maintained drop-in exists yet; tracked
# for removal when nalgebra/simba drop it. Not a vulnerability.
[advisories]
ignore = ["RUSTSEC-2024-0436"]
